#include <iostream>
#include <ctime>
#include <cstdlib>
using namespace std;

int main() {
    const int SIZE = 10;
    int arr[SIZE];

    srand(time(0));

    cout << "Original array: ";
    for(int i = 0; i < SIZE; i++) {
        arr[i] = rand() % 100 + 1;
        cout << arr[i] << " ";
    }
    cout << endl;

    int *ptr = arr;

    cout << "\nPointer access: ";
    for(int i = 0; i < SIZE; i++) {
        cout << *(ptr + i) << " ";
    }
    cout << endl;

    int *minPtr = arr;
    int *maxPtr = arr;

    for(int i = 1; i < SIZE; i++) {
        if(*(arr + i) < *minPtr) {
            minPtr = arr + i;
        }
        if(*(arr + i) > *maxPtr) {
            maxPtr = arr + i;
        }
    }

    cout << "\nMinimum element: " << *minPtr
         << " (index " << minPtr - arr << ")" << endl;
    cout << "Maximum element: " << *maxPtr
         << " (index " << maxPtr - arr << ")" << endl;

    int sum = 0;
    int *current = arr;
    for(int i = 0; i < SIZE; i++) {
        sum += *current;
        current++;
    }
    cout << "Sum of elements: " << sum << endl;

    cout << "\nEven numbers in array: ";
    int evenCount = 0;
    for(int i = 0; i < SIZE; i++) {
        if(*(ptr + i) % 2 == 0) {
            cout << *(ptr + i) << " ";
            evenCount++;
        }
    }
    cout << "\nCount of even numbers: " << evenCount << endl;

    cout << "\nArray in reverse order: ";
    int *start = arr;
    int *end = arr + SIZE - 1;

    while(start < end) {
        int temp = *start;
        *start = *end;
        *end = temp;
        start++;
        end--;
    }

    for(int i = 0; i < SIZE; i++) {
        cout << arr[i] << " ";
    }
    cout << endl;

    cout << "\nPrime numbers in array: ";
    for(int i = 0; i < SIZE; i++) {
        int num = *(ptr + i);
        bool isPrime = true;

        if(num < 2) {
            isPrime = false;
        } else {
            for(int j = 2; j * j <= num; j++) {
                if(num % j == 0) {
                    isPrime = false;
                    break;
                }
            }
        }

        if(isPrime) {
            cout << num << " ";
        }
    }
    cout << endl;

    return 0;
}
